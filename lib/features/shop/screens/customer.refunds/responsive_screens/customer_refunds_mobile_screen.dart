import 'package:flutter/widgets.dart';

class CustomerRefundsMobileScreen extends StatelessWidget {
  const CustomerRefundsMobileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
